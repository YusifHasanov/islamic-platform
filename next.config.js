/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.ytimg.com','medrese.vercel.app','hayalhanem.com'], // Buraya izin verilen domaini ekleyin
    },
};

export default nextConfig;
